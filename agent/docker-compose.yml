services:
  webcheck-agent:
    build:
      context: ..
      dockerfile: agent/Dockerfile
    restart: always
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
